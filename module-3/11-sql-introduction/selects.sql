SELECT city_name`city_name` FROM `cities`;
-- returs the city names in out quires 

SELECT * FROM `cities` limit 3;
-- only returns 3 results 

Select * from  `cities` Where CID = 4;
-- returns all info for the city owith an id of 4 

Select * from `cities` WHERE province = 'mb';